<% for (item of items) { %>

:::: {<%= item['status']==='unpublished' ? '.callout-warning' : '.callout-note' %> icon=false collapse=false}
## <%= item.title %>

<%= item.authors.map(author => author === "Siddharth Vishwanath" ? `[Siddharth Vishwanath]{style="color: #005A9C80;"}` : author).join(", ") %>
<br>
[_<%= item.journal %>. (<%= item.date %>)_]{style="text-align: left;"}
[<% if ((item.info || []).length > 0) { %>
<%= item.info.map(info=> `[[${info.name}](${info.url})]`).join(", ") %>
<% } %>]{style="text-align: right;"}

::::

<% } %>